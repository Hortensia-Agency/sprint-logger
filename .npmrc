# Public packages by default. Provenance is enforced in CI (release.yml passes
# --provenance explicitly via OIDC) — NOT here, because provenance can only be
# generated inside a supported CI provider, so a local `npm publish` would fail.
access=public

# Supply-chain hardening for local dev installs.
audit=true
fund=false
