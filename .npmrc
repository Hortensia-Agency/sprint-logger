# Public, attested publishes. provenance is also passed explicitly in CI; this
# is the belt-and-braces default so a manual publish can't forget it.
provenance=true
access=public

# Supply-chain hardening for local dev installs.
audit=true
fund=false
